#include<stdio.h>
#include<math.h>
int main ()
{
      int x;
      printf("The size of int is = %d",sizeof(x));

      double y;
      printf("The size of double is = %d",sizeof(y));


    return 0;
}